{
  "name": "local-diabetic",
  "private": true,
  "description": "LocalDiabetic member front door \u2014 static, no dependencies",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html style.css assets robots.txt sitemap.xml llms.txt dist/ && echo built"
  }
}