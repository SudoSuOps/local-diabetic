{
  "name": "local-diabetic",
  "private": true,
  "description": "LocalDiabetic member front door — static, no dependencies",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html style.css assets dist/ && echo built"
  }
}
